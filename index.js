/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PullUpChallenge from './components/PullUpChallenge';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PullUpChallenge);
